#ifndef SENSOR_H
#define SENSOR_H

/* Sensor domain — vertical slice.
 *   sensor_model.h — SensorData type, RP2040 parse, cache
 *   sensor_view.h  — LVGL label subscriptions
 */
#include "sensor_model.h"
#include "sensor_view.h"

#endif /* SENSOR_H */
